- reference ||= BillingReference.new
- tag_list ||= []

.modal-wrapper
  .modal-header
    %button.close{:type=>"button","data-dismiss"=>"modal"}
      &times;

    %h3 Billing Reference

  = simple_form_for [@event, reference] do |f|
    = f.hidden_field :name
    .modal-body
      .in-between
      %h4 #{reference.name}
      .in-between
      / = f.input :tag_list, :collection => tag_list, label: "Choice"
      = select_tag "billing_reference[tag_list]", options_for_select(tag_list, reference.tag_list.count >= 1 ? reference.tag_list.first : "")

    .modal-footer
      = f.submit (reference.id.nil? ? "Create Billing Reference" : "Save Billing Reference"), :class => "btn btn-primary"




  